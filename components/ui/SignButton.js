import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const SignButton = ({ icon, color, size, onPress }) => {
  return (
    <View>
      <Text>SignButton</Text>
    </View>
  );
};

export default SignButton;

const styles = StyleSheet.create({});
