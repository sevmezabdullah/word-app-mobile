import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const LoginInput = ({ placeHolder, value, setValue }) => {
  return (
    <View>
      <Text>LoginInput</Text>
    </View>
  );
};

export default LoginInput;

const styles = StyleSheet.create({});
